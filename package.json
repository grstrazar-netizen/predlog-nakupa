{
  "name": "predlog-nakupa-drobnega-materiala",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/verify.mjs --check-only",
    "dev": "node server.mjs",
    "preview": "node server.mjs --dist",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "description": "Document-first local PWA for purchase proposals and material issue documents."
}
